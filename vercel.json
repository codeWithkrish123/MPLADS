{
  "version": 2,
  "buildCommand": "npm run build --prefix MPLADS-UI",
  "installCommand": "npm install --legacy-peer-deps --prefix MPLADS-UI",
  "outputDirectory": "MPLADS-UI/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
